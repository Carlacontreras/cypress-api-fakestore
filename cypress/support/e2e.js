// This file runs before every spec file.
// It only wires up the custom commands/helpers - no global hooks are needed
// for an API suite (there is no browser state to reset between tests).
import "./commands";
