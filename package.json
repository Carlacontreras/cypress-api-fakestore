{
  "name": "cypress-api-fakestore",
  "version": "1.0.0",
  "private": true,
  "description": "API test suite (Cypress) for Fake Store API - https://fakestoreapi.com",
  "scripts": {
    "cy:open": "cypress open",
    "test": "cypress run",
    "test:headless": "cypress run --browser electron --headless",
    "test:chrome": "cypress run --browser chrome"
  },
  "devDependencies": {
    "cypress": "^13.15.0"
  }
}
